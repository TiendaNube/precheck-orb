description: >
  This command validate the name to use in k8s namespace.
parameters:
  name:
    type: string
    description: "Repository or Branch Name for naming validation"
steps:
  - run:
      environment:
        BRANCH_NAME: <<parameters.name>>
      name: Naming Validation
      command: <<include(scripts/test_naming_validation.py)>>