description: Naming Validation, validate the name to use in k8s namespace

executor: default

parameters:
  name:
    type: string
    description: "Repository or Branch Name for naming validation"

steps:
  - naming-validation:
      name: << parameters.name >>
